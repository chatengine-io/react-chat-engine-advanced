import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { ChatHeader } from '.';

<Meta
  title="ChatEngine/ChatFeed/ChatHeader"
  component={ChatHeader}
  argTypes={{
    chatHeaderStyle: { table: { category: 'styles' } },
    chatHeaderTitleStyle: { table: { category: 'styles' } },
    chatHeaderSubtitleStyle: { table: { category: 'styles' } },
    chatHeaderMobileOptionStyle: { table: { category: 'styles' } },
  }}
/>

export const Template = (args) => <ChatHeader {...args} />;

# Chat Header

The Chat Header lives at the top of the Chat Feed and shows data about the Chat you have opened.

Things such as the chat title, members, and last sent message (or last message datetime) may be present.

## Default

<Story
  name="Default"
  args={{
    id: 1,
    title: 'Magic Club',
    description: 'Where the magic happens...',
  }}
>
  {Template.bind({})}
</Story>

<ArgsTable story="Default" />

## Render Functions

You can replace the component with your won code using the `render*` functions.

<Canvas>
  <Story
    name="Render Functions"
    args={{
      title: 'Chat Title',
      renderChatHeader: (props) => <div>Your code here: {props.title}</div>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom Styles

You can customize the look and feel with the `*Style` props.

<Canvas>
  <Story
    name="Custom Styles"
    args={{
      id: 1,
      title: 'Magic Club',
      description: 'Where the magic happens...',
      chatHeaderStyle: { border: '3px solid red' },
      chatHeaderTitleStyle: { border: '3px solid blue' },
      chatHeaderSubtitleStyle: { border: '3px solid green' },
      chatHeaderMobileOptionStyle: { border: '3px solid orange' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
