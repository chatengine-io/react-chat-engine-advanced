import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { File } from '.';
import { Props } from './props';

<Meta title="Components/File" component={File} />

export const Template = (args) => <File {...args} />;

# Image

The Image component represents any Message Attachment which cannot be put into an `<img />` tag.

## Default

<Story
  name="Default"
  args={{
    url: 'https://chat-engine-assets.s3.amazonaws.com/click.mp3',
  }}
>
  {Template.bind({})}
</Story>

<ArgsTable story="Default" />

## Is Loading

If you do not set an `url` the image is considered loading. And will show the following loading `.gif`.

<Story name="Is Loading" args={{}}>
  {Template.bind({})}
</Story>

## File Name

If you do not set an `url` the image is considered loading. And will show the following loading `.gif`.

<Story
  name="File Name"
  args={{
    url: 'https://chat-engine-assets.s3.amazonaws.com/click.mp3',
    fileName: '🚨 🚨 Secret Codes',
  }}
>
  {Template.bind({})}
</Story>

## Custom Style

You can customize the look and feel with the `*Style` props and change the CSS for every Image state.

<Story
  name="Custom Style"
  args={{
    url: 'https://chat-engine-assets.s3.amazonaws.com/click.mp3',
    style: { border: '4px solid red' },
    hoveredStyle: { border: '4px solid green' },
  }}
>
  {Template.bind({})}
</Story>
