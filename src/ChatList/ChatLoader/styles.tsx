import { Properties } from 'csstype';
export const styles = {
  chatLoader: {
    textAlign: 'center',
    backgroundColor: '#e2e2e2',
    margin: '4px',
    borderRadius: '4px',
  } as Properties,
};
